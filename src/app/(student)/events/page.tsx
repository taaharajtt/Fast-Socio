import { PlaceholderScreen } from "@/components/placeholder-screen";

export default function EventsPage() {
  return (
    <PlaceholderScreen
      title="Events"
      subtitle="What's on around campus"
      phase="Phase 6 (Events)"
    />
  );
}

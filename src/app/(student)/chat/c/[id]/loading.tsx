/**
 * A community conversation is the DM conversation screen with a different
 * subject, so it shimmers with the exact same skeleton.
 */
export { default } from "@/app/(student)/chat/[id]/loading";
